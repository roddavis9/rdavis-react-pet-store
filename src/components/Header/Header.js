import React from 'react';

import './Header.css';

const header = (props) => (
    <header className={`Header`}>
        <div>hamburger</div>
        <div>logo</div>
        <nav>
            ...
        </nav>
    </header>
);


export default header;
